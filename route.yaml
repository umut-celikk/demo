apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nginx-demo
  namespace: nova-test
  labels:
    app: nginx-demo
spec:
  to:
    kind: Service
    name: nginx-demo
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
